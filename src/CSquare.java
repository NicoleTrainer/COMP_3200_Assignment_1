/**
 * Represents a square, with sideLength.
 * Extends the CRectangle class, which represents a rectangle.
 */
class CSquare extends CRectangle {

    /**
     * Constructs a square with the specified side length.
     * @param sideLength The side length of the square.
     */
    public CSquare(int sideLength) {

        super(sideLength, sideLength);

    }


}
