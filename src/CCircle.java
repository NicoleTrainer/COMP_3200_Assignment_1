/**
 * Represents shape circle, with radius.
 * Extends COval class, which represents an oval.
 */
class CCircle extends COval {

    /**
     * Constructs a circle with specified radius
     *
     * @param radius The radius of the circle.
     */
    public CCircle(int radius) {
        super(radius, radius);
    }


}
